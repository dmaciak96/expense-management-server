spring:
  application:
    name: expense-management-server
  threads:
    virtual:
      enabled: true
  mongodb:
    uri: ${DB_URI}

security:
  jwt:
    secret: ${JWT_SECRET}
    expiration: ${JWT_TOKEN_EXPIRATION_IN_SECONDS}
